# Environment variables
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated manuscripts, checkpoints and raw model responses
runs/

# Studio project data (per-project JSON, written by services/projectDataServer.ts
# in dev). Personal manuscripts, not source: a repository is not a database.
data/

# Serena agent workspace
.serena/

# Local model weights served to the browser (~780MB). Fetched once by
# scripts/warm-models.ts, never committed: a repository is not a CDN, and these
# files are byte-identical to what Hugging Face already serves.
public/models/
