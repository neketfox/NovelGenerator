export const GEMINI_MODEL_NAME = 'gemini-3.6-flash';
// A floor of 1 allows starting from a single chapter/section and continuing manually in the
// Studio editor afterward; the form's own default (see hooks/useBookGenerator.ts) stays 3.
export const MIN_CHAPTERS = 1;